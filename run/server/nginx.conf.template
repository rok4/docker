upstream rok4server { server middle:9000; }
                                               
server {
    listen 80 default_server;

    location /${ROKSERVER_PREFIX} {
        rewrite /${ROKSERVER_PREFIX}/?(.*) /$1 break;
        fastcgi_pass rok4server;
        include fastcgi_params;
    }
}